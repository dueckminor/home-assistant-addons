# The gen folder is for all kind of generated stuff and has to be ignored
gen

# Files generated during debugging of Go projects
__debug_*

# Files required for docker build will be copied to the addons/* directory
addons/*/dist
addons/*/*-amd64
addons/*/*-arm64

# Dependencies of the JS projects
web/**/node_modules

# Generated files of the JS projects which are embedded in the Go projects
go/**/dist

# Font files (generated by the JS projects during build)
*.woff*
*.ttf
*.eot
